from django.apps import AppConfig

class ItemConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "item"  # This should be the name of your app, which seems to be "item"
